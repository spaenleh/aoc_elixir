import Config

config :aoc,
  # The prefix is used when creating solutions and test modules with `mix aoc.create`.
  prefix: AocElixir.Solutions,

  # Optional

  # Include help comments when generating modules and tests
  generate_comments: true
